name: ErrorResponse
description: An error response
contentType: application/json
schema:
  type: object
  required:
    - message
  properties:
    message:
      type: string
      description: A mesage of what went wrong